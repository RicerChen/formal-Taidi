# 代码核心流程详细描述

## 一、数据处理流程

### 1.1 任务一：PDF财务报告表格提取

**代码来源**：`3_15-2-副本.ipynb`

**处理流程**：

1. **文件遍历**：遍历`示例数据/附件2：财务报告`目录下的PDF文件
2. **PDF解析**：使用`pdfplumber`库解析PDF文件，提取页面中的表格
3. **表格识别**：识别表格边界，处理合并单元格，合并跨页表格
4. **字段映射**：将提取的字段映射到标准字段名（精确匹配→编辑距离模糊匹配→语义相似度匹配）
5. **数据清洗**：使用`normalize_text()`函数清洗文本（去除多余空格、标准化符号等）
6. **数据校验**：进行报表勾稽、跨表一致性、历史异常等校验
7. **数据库入库**：将处理后的数据导入SQLite数据库

**核心函数**：
- `process_one_shenjiao_pdf_without_Q3()`：处理深交所财务报告PDF
- `process_one_shangjiao_pdf_without_Q3()`：处理上交所财务报告PDF
- `normalize_text()`：文本标准化
- `process_df_without_Q3()`：处理提取的DataFrame

### 1.2 数据清洗逻辑

```python
def normalize_text(s):
    if s is None:
        return ""
    s = str(s)
    s = s.replace("\n", " ").replace("\r", " ").replace("\t", " ")
    s = s.replace("（", "(").replace("）", ")")
    s = s.replace("：", ":").replace("，", ",")
    s = s.replace(""", '"').replace(""", '"')
    s = s.replace("'", "'").replace("'", "'")
    s = re.sub(r"\s+", " ", s).strip()
    return s
```

### 1.3 数据库初始化

```python
def init_database():
    engine = create_engine("sqlite:///example.db")

    def normalize_stock_code(x):
        if pd.isna(x):
            return None
        s = str(x).strip()
        if not s:
            return None
        s = re.sub(r"\.0+$", "", s)
        if s.isdigit():
            s = s.zfill(6)
        return s

    def cast_df_by_table(df, table_name):
        df = df.copy()
        dtype_map = TABLE_DTYPE_MAP.get(table_name, {})
        for col, sql_type in dtype_map.items():
            if col not in df.columns:
                continue
            if col == "stock_code":
                df[col] = df[col].apply(normalize_stock_code).astype("string")
            else:
                df[col] = df[col].astype("string")
        return df

    # 读取Excel文件
    sheets = pd.read_excel(
        r"final_result_final.xlsx",
        sheet_name=None,
        engine="openpyxl"
    )

    for sheet_name, df in sheets.items():
        table_name = sheet_name.strip().replace(" ", "_").replace("-", "_")
        df = cast_df_by_table(df, table_name)
        df.to_sql(
            table_name,
            con=engine,
            if_exists="replace",
            index=False
        )

    return SQLDatabase.from_uri("sqlite:///example.db")
```

---

## 二、SQL生成逻辑

### 2.1 任务二：自然语言到SQL转换

**代码来源**：`test_system.py`

**处理流程**：

1. **状态初始化**：创建`ParentState`对象，存储查询状态
2. **路由决策**：调用`route_node()`判断查询类型（sql或clarify）
3. **SQL生成**：调用`run_sql_query()`生成并执行SQL
4. **结果处理**：将SQL查询结果转换为自然语言回答

### 2.2 状态定义

```python
class ParentState:
    def __init__(self):
        self.messages = []      # 消息历史
        self.query = ""         # 原始查询
        self.real_query = ""    # 处理后的查询
        self.route = ""         # 路由决策（sql/clarify）
        self.reason = ""        # 路由原因
        self.missing_slots = [] # 缺失的槽位
        self.need_sql = False   # 是否需要SQL
        self.need_clarification = False  # 是否需要澄清
        self.sql_result = {}    # SQL结果
        self.final_answer = ""  # 最终回答
        self.sql_query = ""     # 生成的SQL
```

### 2.3 路由决策逻辑

**核心流程**：

```python
def route_node(state):
    question = state.real_query

    route_prompt = f"""
    你是一个上市公司财报智能问数助手的"路由器（Router）"。

    你的任务不是回答用户问题，而是判断这个问题应该走哪条处理链路。

    你只能输出以下两种 route：
    1. sql
       - 适用于可以主要通过结构化财务数据库回答的问题
       - 典型包括：数值查询、指标查询、同比/环比、趋势、排名、topN、筛选、排序、聚合统计、跨公司比较
    2. clarify
       - 适用于用户问题缺少关键条件，当前无法可靠执行
       - 例如缺少公司名、报告期、指标名、比较对象等

    判定原则：
    - 如果问题主要在问"多少、是否、排名、变化趋势、同比环比、topN、哪个最大/最小"，优先判为 sql
    - 如果问题缺少关键查询条件，判为 clarify

    请特别注意：
    - "趋势分析""同比分析""排名分析"通常仍然属于 sql
    """

    structured_llm = llm.with_structured_output(RouteDecision)
    try:
        decision = structured_llm.invoke(route_prompt)
        state.route = decision.route
        state.reason = decision.reason
        state.missing_slots = decision.missing_slots
        state.need_sql = decision.route == "sql"
        state.need_clarification = decision.route == "clarify"
    except Exception as e:
        print(f"路由决策错误: {e}")
        state.route = "clarify"
        state.reason = "路由决策失败"
        state.missing_slots = ["unknown"]
        state.need_sql = False
        state.need_clarification = True

    return state
```

**路由决策数据结构**：

```python
class RouteDecision(BaseModel):
    route: Literal["sql", "clarify"] = Field(
        description="The routing decision for the user query."
    )
    reason: str = Field(
        description="One-sentence reason for the routing decision."
    )
    missing_slots: List[str] = Field(
        default_factory=list,
        description="Missing required slots such as company, period, metric. Empty list if none."
    )
```

### 2.4 澄清函数

```python
def clarify(state):
    question = state.real_query
    missing_slots = state.missing_slots

    clarify_prompt = f"""
        你是一个上市公司财报智能问数助手。

        用户原始问题：
        {question}

        当前还缺少这些关键信息：
        {missing_slots}

        你的任务是：
        基于用户原始问题和缺失槽位，生成一句自然、简洁、礼貌的澄清问题，引导用户一次性补全所缺信息，以便继续查询。

        要求：
        1. 只输出一句面向用户的澄清问题，不要输出解释，不要输出 JSON，不要输出多余内容。
        2. 如果缺少多个槽位，尽量合并成一句话一起问，不要拆成多句。
        3. 语气自然，适合中文对话场景。
        4. 问法要贴合财报问数场景。
        """

    try:
        response = llm.invoke([{"role": "user", "content": clarify_prompt}])
        state.final_answer = response.content
    except Exception as e:
        print(f"澄清错误: {e}")
        state.final_answer = "抱歉，我无法理解您的问题，请提供更多信息。"

    return state
```

### 2.5 SQL生成与执行

**核心流程**：

```python
def run_sql_query(state, db):
    query = state.real_query

    # 初始化工具
    toolkit = SQLDatabaseToolkit(db=db, llm=llm)
    tools = toolkit.get_tools()
    get_schema_tool = next(tool for tool in tools if tool.name == "sql_db_schema")
    run_query_tool = next(tool for tool in tools if tool.name == "sql_db_query")

    # 生成SQL查询
    generate_query_system_prompt = """
    你是一个专为与 SQL 数据库交互而设计的智能体。
    给定一个用户输入问题，请生成一条语法正确的 sqlite 查询语句并执行，
    然后查看查询结果并返回答案。除非用户明确指定了希望获取的示例数量，否则始终将你的查询结果限制在最多 5 条。

    你可以按相关列对结果进行排序，以返回数据库中最有趣的示例。切勿查询特定表的所有列，仅根据问题请求相关的列。

    对于趋势/可视化类问题（例如：趋势、变化、走势、近几年、近年、历年、近年来、多年、可视化、绘图、折线图、柱状图）：
    1. 返回时间序列查询，而非单个标量结果。
    2. 选择一个时间列和一个数值指标列。
    3. 优先保留原始字段名，除非确有必要，否则不要统一别名为 x_axis 和 y_axis。
    4. 按时间顺序升序排列结果。
    5. 不要机械地应用默认的 top_k 限制；应保留绘图所需的所有时间段。
    """

    # 获取数据库schema
    schema_prompt = "请提供所有表的schema信息"
    llm_with_schema = llm.bind_tools([get_schema_tool], tool_choice="any")
    schema_response = llm_with_schema.invoke([HumanMessage(content=schema_prompt)])

    # 生成SQL查询
    sql_prompt = f"""
    数据库schema：
    {schema_response.content}

    用户问题：{query}

    请生成SQL查询语句并执行，然后返回答案。
    """

    llm_with_query = llm.bind_tools([run_query_tool], tool_choice="any")
    try:
        sql_response = llm_with_query.invoke([HumanMessage(content=sql_prompt)])
        # 提取SQL查询结果
        if hasattr(sql_response, "tool_calls") and sql_response.tool_calls:
            tool_call = sql_response.tool_calls[0]
            normalized_call = normalize_tool_call(tool_call)
            if normalized_call["name"] == "sql_db_query":
                state.sql_query = normalized_call["args"].get("query", "")
                # 执行查询
                from langchain_community.tools.sql_database.tool import QuerySQLDataBaseTool
                query_tool = QuerySQLDataBaseTool(db=db)
                sql_result = query_tool.run(state.sql_query)
                state.sql_answer = sql_result
                # 生成自然语言回答
                answer_prompt = f"""
                SQL查询结果：
                {sql_result}

                用户问题：{query}

                请将查询结果转换为自然、友好的中文回答。
                """
                answer_response = llm.invoke([HumanMessage(content=answer_prompt)])
                state.final_answer = answer_response.content
            else:
                state.final_answer = "无法生成SQL查询"
        else:
            state.final_answer = sql_response.content
    except Exception as e:
        print(f"SQL查询错误: {e}")
        state.final_answer = f"查询出错: {str(e)}"

    return state
```

### 2.6 SQL生成Prompt关键规则

**时间粒度选择规则**：

```python
# A. 如果问题是"跨年度趋势"
# 优先使用 report_year 作为时间轴
# 年度口径优先匹配 FY、年度、年报、12-31、1231、年末
# 最终结果必须保证每个 report_year 最多一条记录

# B. 如果问题是"某一年内的季度/期间变化"
# 应优先使用 report_period
# 必要时将 report_year 和 report_period 拼接成一个唯一时间标签
# 结果可按 Q1 < H1 < Q3 < FY 的顺序排序
```

**指标字段优先级规则**：

```python
# 1. 当用户明确问"同比增长率""环比增长率""增长率"时：
#    - 如果 schema 中存在对应的现成增长率字段，必须优先直接查询该字段
#    - 不要改查原始金额字段后再自行计算

# 2. 典型映射：
#    - 营业总收入环比增长率 -> operating_revenue_qoq_growth
#    - 营业总收入同比增长率 -> operating_revenue_yoy_growth
#    - 净利润同比增长率 -> net_profit_yoy_growth

# 3. 只有当对应增长率字段不存在时，才允许改查原始金额字段并自行计算
```

**累计口径转单季规则**：

```python
# 1. Q1单季 = Q1累计
# 2. Q2单季 = H1累计 - Q1累计
# 3. Q3单季 = Q3累计
# 4. Q4单季 = FY累计 - H1累计 - Q3累计
```

**股票代码字段规则**：

```python
# - stock_code 一律按字符串处理，不要按数字处理
# - 查询股票代码时，必须使用带引号的 6 位代码
# - 例如 '000538'、'600436'
# - 不要写成 stock_code = 538 或 stock_code = 600436
```

### 2.7 主处理函数

```python
def process_query(query, db):
    start_time = time.time()

    state = ParentState()
    state.query = query
    state.real_query = query

    # 路由决策
    state = route_node(state)

    if state.need_sql:
        state = run_sql_query(state, db)
    else:
        state = clarify(state)

    end_time = time.time()
    response_time = end_time - start_time

    return {
        "answer": state.final_answer,
        "response_time": response_time,
        "sql_query": state.sql_query,
        "route": state.route
    }
```

---

## 三、RAG流程

### 3.1 任务三：RAG知识库构建与问答

**代码来源**：`任务三1.0.ipynb`、`任务三3.0.ipynb`

**处理流程**：

1. **文档加载**：使用`PyMuPDFLoader`加载研报PDF
2. **文本提取**：提取PDF中的文本和表格内容
3. **文本分割**：使用`RecursiveCharacterTextSplitter`分割文本
4. **向量嵌入**：使用`HuggingFaceBgeEmbeddings(model_name="BAAI/bge-m3")`生成嵌入
5. **向量存储**：使用Milvus向量数据库存储
6. **语义检索**：根据用户问题检索相关文档
7. **答案生成**：基于检索结果生成回答

### 3.2 RAG知识库构建核心代码

```python
from langchain_openai import ChatOpenAI

llm = ChatOpenAI(
    model="glm-5",
    temperature=0,
    base_url="https://dashscope.aliyuncs.com/compatible-mode/v1",
)
```

**文本分割**：

```python
from langchain_text_splitters import MarkdownHeaderTextSplitter, RecursiveCharacterTextSplitter

text_splitter = RecursiveCharacterTextSplitter.from_tiktoken_encoder(
    model_name="BAAI/bge-m3",
    chunk_size=512,
    chunk_overlap=50
)
```

**向量嵌入**：

```python
from langchain_community.embeddings import HuggingFaceBgeEmbeddings

embeddings = HuggingFaceBgeEmbeddings(
    model_name="BAAI/bge-m3",
    query_instruction="为这个句子生成表示以用于检索相关文章："
)
```

**向量存储**：

```python
from langchain_community.vectorstores import Milvus

vectorstore = Milvus(
    embedding_function=embeddings,
    collection_name="research_reports",
    connection_args={"host": "localhost", "port": "19530"}
)

# 文档存储
vectorstore.add_documents(documents=all_splits)
```

### 3.3 RAG检索与问答

```python
def retrieve_relevant_docs(query, vectorstore, k=5):
    """检索相关文档"""
    docs = vectorstore.similarity_search(query, k=k)
    return docs

def generate_answer(query, retrieved_docs, llm):
    """基于检索结果生成回答"""
    context = "\n".join([doc.page_content for doc in retrieved_docs])
    prompt = f"""
    基于以下检索到的信息回答用户问题。

    检索信息：
    {context}

    用户问题：{query}

    请生成一个准确、完整的回答，并标注信息来源。
    """
    response = llm.invoke([{"role": "user", "content": prompt}])
    return response.content
```

### 3.4 任务调度系统

**状态机设计**（借鉴OS进程调度）：

```python
class ParentState(MessagesState, total=False):
    messages: List[BaseMessage]  # 消息历史
    sub_tasks: List[Dict]  # 子任务列表
    task_status: str  # 任务状态：ready/running/done/failed
    results: Dict  # 执行结果
```

**调度决策**：

```python
def scheduler_route(state: ParentState) -> Literal["sql_node", "rag_node", "clarify", "merge_generate"]:
    """根据查询类型路由到不同处理节点"""
    # SQL_QUERY: 从结构化数据库获取数值数据
    # RAG_QUERY: 从研报知识库检索文本信息
    # merge_generate: 融合生成综合分析结论
```

### 3.5 表格类图像处理（任务三3.0）

```python
# 使用MinerU处理研报中的表格图像
# 1. 加载PDF并识别表格区域
# 2. 识别表格外标题（如"图表8：..."）
# 3. 裁剪包含标题的表格图像
# 4. 提取表格内容并转为文本
# 5. 纳入向量知识库
```

---

## 四、核心流程图

### SQL查询处理流程图

```
用户查询
    ↓
┌─────────────────────────────────────┐
│           路由决策 (route_node)      │
│  判断：sql / clarify                │
└─────────────────────────────────────┘
    ↓
┌────────────────┐    ┌────────────────┐
│  sql路由        │    │  clarify路由   │
│  run_sql_query │    │  clarify()    │
│  - 获取schema   │    │  生成澄清问题  │
│  - 生成SQL     │    └────────────────┘
│  - 执行查询    │
│  - 转换回答    │
└────────────────┘
    ↓
┌─────────────────────────────────────┐
│            返回最终回答              │
└─────────────────────────────────────┘
```

### RAG流程图

```
研报PDF
    ↓
┌─────────────────────────────────────┐
│           文档加载与处理            │
│  PyMuPDFLoader + RecursiveCharacter │
│  TextSplitter                        │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│           向量嵌入与存储             │
│  HuggingFaceBgeEmbeddings + Milvus  │
└─────────────────────────────────────┘
    ↓
用户查询 → 语义检索 → 相关文档 → LLM生成回答
```

### 系统整体架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                         用户交互层                               │
│                    自然语言查询 / 回答展示                        │
└─────────────────────────────────────────────────────────────────┘
                                ↓
┌─────────────────────────────────────────────────────────────────┐
│                         服务层                                  │
│    ┌──────────────┐    ┌──────────────┐    ┌──────────────┐    │
│    │  路由决策     │    │  任务调度     │    │  结果生成     │    │
│    └──────────────┘    └──────────────┘    └──────────────┘    │
└─────────────────────────────────────────────────────────────────┘
                    ↓                           ↓
┌─────────────────────────────┐    ┌─────────────────────────────┐
│      NL2SQL引擎              │    │      RAG引擎                │
│  ┌─────────────────────────┐ │    │  ┌─────────────────────────┐│
│  │ SQLDatabaseToolkit       │ │    │  │ 文档加载                 ││
│  │ + LangChain              │ │    │  │ 文本分割                 ││
│  └─────────────────────────┘ │    │  │ 向量嵌入                 ││
│                             │    │  │ Milvus检索               ││
└─────────────────────────────┘    │  └─────────────────────────┘│
                                   └─────────────────────────────┘
                    ↓                           ↓
┌─────────────────────────────┐    ┌─────────────────────────────┐
│      SQLite数据库            │    │      Milvus向量数据库        │
│    （结构化财务数据）         │    │    （研报知识库）            │
└─────────────────────────────┘    └─────────────────────────────┘
```

---

## 五、技术栈总结

| 模块 | 技术/工具 | 说明 |
|------|----------|------|
| PDF解析 | pdfplumber、MinerU | 财务报告PDF表格提取 |
| 数据库 | SQLite | 结构化财务数据存储 |
| LLM框架 | LangChain、LangGraph | 自然语言处理和状态机 |
| 向量数据库 | Milvus | 研报知识向量存储 |
| 嵌入模型 | BAAI/bge-m3 | 中文语义嵌入生成 |
| 大语言模型 | glm-5、kimi-k2.5 | 自然语言理解和生成 |
| 文本分割 | RecursiveCharacterTextSplitter | 语义感知的文本分块 |

---

## 六、关键配置参数

| 参数 | 值 | 说明 |
|------|-----|------|
| chunk_size | 512 | 文本分割块大小 |
| chunk_overlap | 50 | 文本块重叠大小 |
| vector_similarity_k | 5 | 检索返回的Top-K文档数 |
| max_sql_results | 5 | SQL查询结果上限 |
| embedding_dimension | 1024 | 嵌入向量维度 |
| collection_name | research_reports | Milvus集合名称 |
